helpful commands:

gcloud init 
gcloud info

gcloud config list
gcloud auth list
gcloud compute instances list

gcloud compute instances start [VM_NAME]
gcloud compute instances stop [VM_NAME]

gcloud compute ssh [VM_NAME]

**** important ones ****




*********

gcloud config set project madison313
gcloud config set compute/zone us-central1-a


gcloud compute instances start dee-vm --zone=us-central1-a --project=madison313

gcloud compute instances stop dee-vm --zone=us-central1-a

gcloud compute instances list \
  --filter="name=dee-vm" \
  --format="table(name,zone,machineType.basename(),networkInterfaces[0].networkIP:label=INTERNAL_IP,networkInterfaces[0].accessConfigs[0].natIP:label=EXTERNAL_IP,status)"

RUN THIS BELOW COMMAND TO WORK IN COMMAND LINE TERMINAL.
gcloud compute ssh dee-vm --zone=us-central1-a --project=madison313 


********

gcloud compute config-ssh --force-key-file-overwrite

ssh dee-vm.us-central1-a.madison313


gcloud compute scp --recurse INSTANCE_NAME:~/remote-folder-path ~/local-destination-path

INSTANCE_NAME: dee-vm

